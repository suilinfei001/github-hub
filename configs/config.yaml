base_url: "http://localhost:8080"
token: ""
endpoints:
  # 若你的服务端路径不同，请按需修改以下模板；支持占位 {repo} / {branch} / {path}
  download: "/api/v1/download"          # 也可用 "/api/v1/repos/{repo}/archive"
  branch_switch: "/api/v1/branch/switch" # 也可用 "/api/v1/repos/{repo}/branch/{branch}"
  dir_list: "/api/v1/dir/list"           # 也可用 "/api/v1/dir/{path}"
  dir_delete: "/api/v1/dir"              # 也可用 "/api/v1/dir/{path}"

